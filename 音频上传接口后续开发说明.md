# 音频自动上传脚本开发说明

## 目标

本说明用于开发本地自动上传脚本，把本地生成的 MP3 文件上传到线上 `audio-autoplay-skip-web` 项目。

脚本不使用后台账号密码，不调用 JWT 登录接口，只使用后台生成的上传 Token。

## 上传接口

基础地址：

```text
https://你的域名/api/v1/
```

上传接口：

```text
POST /api/v1/audios/upload/
```

完整示例：

```text
https://你的域名/api/v1/audios/upload/
```

认证请求头：

```text
X-Upload-Token: <后台生成的上传 Token>
```

请求格式：

```text
multipart/form-data
```

请求字段：

```text
file      必填，mp3 文件
filename  可选，显示文件名；不传则使用原始 MP3 文件名
duration  可选，音频时长，单位秒
cover     可选，封面图，支持 jpg/jpeg/png/webp，最大 100KB
```

不要传 `group`。上传接口会根据 Token 绑定的分组自动保存到对应分组。

## Token 创建

1. 进入 Django Admin 后台。
2. 打开：

```text
https://你的域名/admin/audios/audiouploadtoken/add/
```

3. 填写 Token 名称。
4. 选择绑定分组。
5. 确认启用状态已勾选。
6. 保存。
7. 复制页面顶部提示里的明文 Token。

注意：

```text
明文 Token 只显示一次。
如果忘记复制，删除旧 Token 后重新创建。
如果怀疑泄露，停用或删除旧 Token 后重新创建。
```

## 脚本配置

建议通过 `.env` 或系统环境变量配置，不要把 Token 写死在代码里。

推荐配置项：

```text
PODCAST_SERVER_URL=https://你的域名
PODCAST_UPLOAD_TOKEN=后台生成的上传Token
PODCAST_OUTPUT_DIR=boke/output
PODCAST_UPLOAD_LOG=upload_records.json
```

如果脚本在 Windows 本地运行，`PODCAST_OUTPUT_DIR` 可以是绝对路径，例如：

```text
PODCAST_OUTPUT_DIR=E:\boke\output
```

## 上传流程

脚本建议按下面流程实现：

1. 读取配置：服务器地址、上传 Token、本地输出目录、上传日志路径。
2. 扫描输出目录里的 `.mp3` 文件。
3. 根据本地上传日志过滤已经成功上传过的文件。
4. 对每个待上传文件调用上传接口。
5. 上传成功后，把文件路径、文件名、文件大小、接口返回的音频 ID 写入本地日志。
6. 上传失败时记录失败原因，不要中断整个批次。
7. 批次结束后输出成功数量、失败数量、跳过数量。

## 去重规则

后端已有防重复逻辑：

```text
同一个 Token 绑定分组下，如果 filename 已存在，接口会拒绝上传。
```

脚本仍然建议维护本地上传日志，避免每次都请求服务器。

推荐本地日志结构：

```json
{
  "E:\\boke\\output\\demo.mp3": {
    "filename": "demo.mp3",
    "size": 12345678,
    "audio_id": 123,
    "uploaded_at": "2026-06-10T15:00:00+08:00"
  }
}
```

脚本判断是否跳过时，建议同时比较：

```text
本地文件绝对路径
文件大小
```

如果同一路径文件大小变化，应该重新尝试上传。

## 返回格式

上传成功：

```json
{
  "code": 201,
  "message": "上传成功",
  "data": {
    "id": 123,
    "filename": "demo.mp3",
    "group": 1,
    "group_name": "掘金社区",
    "duration": 300,
    "file_size": 12345678,
    "upload_time": "2026-06-10T15:00:00+08:00"
  }
}
```

常见失败：

```text
401 上传 Token 不能为空。
401 上传 Token 无效或已停用。
400 只支持上传 mp3 文件。
400 封面只支持 jpg、jpeg、png、webp。
400 音频图片大小不能超过 100KB。
400 当前分组下已存在同名音频。
413 文件过大，通常是 Nginx client_max_body_size 太小。
502/504 后端服务或代理超时。
```

## curl 示例

只上传 MP3：

```bash
curl -X POST "https://你的域名/api/v1/audios/upload/" \
  -H "X-Upload-Token: 你的上传Token" \
  -F "file=@demo.mp3" \
  -F "filename=demo.mp3" \
  -F "duration=300"
```

同时上传封面：

```bash
curl -X POST "https://你的域名/api/v1/audios/upload/" \
  -H "X-Upload-Token: 你的上传Token" \
  -F "file=@demo.mp3" \
  -F "cover=@cover.jpg" \
  -F "filename=demo.mp3" \
  -F "duration=300"
```

## Python 脚本骨架

```python
import json
import os
from datetime import datetime, timezone
from pathlib import Path

import requests


SERVER_URL = os.environ["PODCAST_SERVER_URL"].rstrip("/")
UPLOAD_TOKEN = os.environ["PODCAST_UPLOAD_TOKEN"]
OUTPUT_DIR = Path(os.environ.get("PODCAST_OUTPUT_DIR", "boke/output"))
UPLOAD_LOG = Path(os.environ.get("PODCAST_UPLOAD_LOG", "upload_records.json"))


def load_records():
    if not UPLOAD_LOG.exists():
        return {}
    return json.loads(UPLOAD_LOG.read_text(encoding="utf-8"))


def save_records(records):
    UPLOAD_LOG.write_text(
        json.dumps(records, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )


def should_skip(file_path, records):
    key = str(file_path.resolve())
    record = records.get(key)
    return bool(record and record.get("size") == file_path.stat().st_size)


def upload_file(file_path):
    url = f"{SERVER_URL}/api/v1/audios/upload/"
    headers = {"X-Upload-Token": UPLOAD_TOKEN}
    with file_path.open("rb") as file_obj:
        files = {"file": (file_path.name, file_obj, "audio/mpeg")}
        data = {"filename": file_path.name}
        response = requests.post(url, headers=headers, files=files, data=data, timeout=120)
    response.raise_for_status()
    payload = response.json()
    if payload.get("code") != 201:
        raise RuntimeError(payload)
    return payload["data"]


def main():
    records = load_records()
    success = 0
    skipped = 0
    failed = 0

    for file_path in sorted(OUTPUT_DIR.glob("*.mp3")):
        if should_skip(file_path, records):
            skipped += 1
            continue

        try:
            result = upload_file(file_path)
        except Exception as exc:
            failed += 1
            print(f"[失败] {file_path.name}: {exc}")
            continue

        records[str(file_path.resolve())] = {
            "filename": file_path.name,
            "size": file_path.stat().st_size,
            "audio_id": result["id"],
            "uploaded_at": datetime.now(timezone.utc).isoformat(),
        }
        save_records(records)
        success += 1
        print(f"[成功] {file_path.name} -> #{result['id']}")

    print(f"完成：成功 {success}，跳过 {skipped}，失败 {failed}")


if __name__ == "__main__":
    main()
```

## 服务器要求

上传接口依赖后端迁移：

```bash
python manage.py migrate
```

如果宝塔系统 Python 没有 Django，需要使用宝塔 Python 项目的解释器，例如：

```bash
/www/server/pyporject_evn/versions/3.10.20/bin/python3 manage.py migrate
```

Nginx 建议保留：

```nginx
client_max_body_size 200M;
```

如果上传文件可能超过 200MB，需要同步调大。

## 已确认约定

1. 脚本不保存后台账号密码。
2. 脚本只使用 `X-Upload-Token`。
3. Token 绑定一个固定分组。
4. 脚本不传 group。
5. 后端只允许 MP3 文件。
6. 封面可选，最大 100KB。
7. duration 可选，后端不会自动计算。
8. 同一分组同名音频会被拒绝。
